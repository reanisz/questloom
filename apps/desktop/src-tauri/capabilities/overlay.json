{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Capability for the overlay notification window",
  "windows": ["overlay"],
  "permissions": [
    "core:default",
    "core:window:allow-set-size",
    "opener:allow-open-url",
    "opener:allow-default-urls"
  ]
}
